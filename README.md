# Heat Trust

This project replicates the matching of zipcodes to geolocations using Open Street Maps dataset available through geonames. 

Use the script "geonames_sharing.R" to replicate the mapping process. An R project is also available for easier replication. 
